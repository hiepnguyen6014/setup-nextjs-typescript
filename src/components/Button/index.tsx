import React from "react";
import type { NextPage } from "next";

import styles from "./Button.module.scss";

const Button: NextPage = () => (
  <div className={styles["button-wrapper"]}>success 3</div>
);

export default Button;
